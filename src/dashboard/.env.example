# dashboard — environment configuration

# Public API base URL — this is the only service the dashboard talks to.
# All auth and billing requests go through api-gateway.
ORBITAL_API_URL=http://localhost:4000
NEXT_PUBLIC_ORBITAL_API_URL=http://localhost:4000

# Analytics (optional)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_SENTRY_DSN=
